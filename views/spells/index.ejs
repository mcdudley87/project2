<h2>Spells in this book:</h2>

<% grimoire.spells.forEach(function(spell) { %>
	<p><%= spell.name %></p>
<% }); %>

<h2>Add a spell...</h2>

<% spells.forEach(function(newSpell) { %>
	<p><%= newSpell.name %></p>
<% }); %>
